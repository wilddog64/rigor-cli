# Retrospective — rigor-cli v0.1.6

**Date:** 2026-05-15
**Milestone:** ai-* symlink fix + gist-02 setup script + ai-helpers howto + ai-triage-pod
**PR:** #10 — merged to main
**Participants:** Claude, Copilot

## What Went Well
- Copilot caught 4 real issues: missing `cd` to repo root, broken stdin pipe example, missing changelog entry, wrong update path in howto
- All 4 issues were fixed and threads resolved before merge
- ai-triage-pod helper is a clean extension of the existing pattern

## What Went Wrong
- PR had a merge conflict with main at merge time (duplicate commits on both branches from earlier squash-merge history)
- Required a merge commit to resolve CHANGE.md, memory-bank conflicts before GitHub would allow merge
- Branch created off older SHA — next time verify branch base is current main tip

## Process Rules Added
- Resolve Copilot threads via GraphQL after fix round (same pattern as shopping-cart-infra)

## Decisions Made
- `setup-ai-helpers.sh` always `cd`s to git toplevel after `_check_deps` — prevents wrong-directory symlink creation

## Theme
A polish release that shipped real developer tooling (setup script, howto, pod triage helper). The merge conflict was procedural noise from branch history, not a code quality issue. Copilot's four findings were all documentation/usability gaps rather than logic bugs — a sign the core implementation was solid.
