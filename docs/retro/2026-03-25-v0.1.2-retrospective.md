# Retrospective — v0.1.2

**Date:** 2026-03-25
**Milestone:** v0.1.2 — lib-foundation v0.3.11 subtree pull (YAML IP audit)
**PR:** #5 — merged to main (`5ed7f8d`)
**Participants:** Claude, Copilot

## What Went Well

- **Subtree pull clean** — `git subtree pull` for lib-foundation v0.3.11 applied with no conflicts
- **Smoke test confirmed end-to-end** — `bin/rigor audit` correctly detected `192.168.1.100` in staged `values.yaml` and exited 1 with warning before PR was filed
- **Copilot findings valid and quick to fix** — both findings (docs not updated, PR description inaccurate) were doc-only; fixed in one commit

## What Went Wrong

- **Docs not updated alongside subtree pull** — `bin/rigor` usage text and README still described `audit` as checking `.sh` only; Copilot caught it. Rule: when pulling a subtree that changes observable behavior, update user-facing docs in the same PR.
- **PR description said "pure subtree update"** — inaccurate once CHANGE.md, README, retro, and memory-bank were included. Always review the PR description scope before filing.

## Process Rules Added

| Rule | Where |
|---|---|
| Subtree pull PRs that change audit behavior must update `bin/rigor` usage text and README in the same commit | PR checklist |
| PR description must accurately list all changed scopes — no "pure X" claims when other files are touched | PR template |

## Decisions Made

- **v0.1.2 scope: subtree pull only** — no new rigor-cli features; this is a pure consumer update bringing in lib-foundation's new YAML IP check

## Theme

v0.1.2 was a one-liner milestone: pull the lib-foundation subtree and ship it. The real work was done upstream (lib-foundation v0.3.11). Copilot correctly flagged that the consumer repo's docs hadn't caught up with the new behavior — a reminder that subtree pulls that change observable audit output require doc updates in the same PR, not just a CHANGE.md entry.
