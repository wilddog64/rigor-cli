# Retrospective — v0.3.9 (docs backfill)

**Date:** 2026-03-24
**Milestone:** v0.3.7+v0.3.8 release history backfill + memory-bank reconciliation
**PR:** #15 — merged to main (`fb09921`)
**Participants:** Claude, Copilot

## What Went Well

- Copilot caught the central inconsistency clearly and completely: 7 comments all pointing at the same root cause (memory-bank "no tag" vs tag links in README/docs), no false positives
- Fix was a single commit (`65f761c`) touching only the two memory-bank files — minimal, correct scope
- CI green on both commits; docs-only PR ran fast (14s each)
- All 7 threads replied + resolved in one pass

## What Went Wrong

- The PR was created with memory-bank still reflecting the pre-tagging state — the retroactive tag step (done in `/post-merge` for PR #14) was not reflected in the PR's own commits
- Root cause: `activeContext.md` heading was never updated from `feat/v0.3.8` to `feat/v0.3.9` when the branch was cut; carried the stale heading into PR #15
- "No tag (CHANGE.md [Unreleased])" text was copy-pasted from the original PR #13/14 merge notes without updating after retroactive tagging

## Process Rules Added

| Rule | Where to add |
|------|-------------|
| After retroactive tagging, immediately update memory-bank tag status before cutting next branch | `/post-merge` Step 7 checklist |
| Before `/create-pr`, verify memory-bank heading matches current branch | `/create-pr` Step 5 (memory-bank current) |

## Decisions Made

- `feat/v0.3.9` was a housekeeping-only branch (no new features); version number held at v0.3.8 — no new tag for this PR
- Next branch is `feat/v0.3.10`

## Theme

A one-finding PR that revealed a process gap: retroactive tagging updates the git and GitHub state but doesn't automatically propagate to memory-bank. Copilot caught it; fix was clean. The process rule is simple — after any retroactive tag, update memory-bank before the next branch is cut.
