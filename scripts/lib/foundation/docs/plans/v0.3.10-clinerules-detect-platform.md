# Spec: v0.3.10 — Fix `.clinerules` `_detect_platform` contract

**Repo:** `wilddog64/lib-foundation`
**Branch:** `feat/v0.3.10`
**File:** `.clinerules`

---

## Problem

The `## Key Contracts` section of `.clinerules` documents the wrong return values for
`_detect_platform`:

```
`_detect_platform` returns: `debian | rhel | arch | darwin | unknown`
```

The actual return values (verified in `scripts/lib/system.sh:749` and
`scripts/tests/lib/core.bats`) are:

```
mac | wsl | debian | redhat | linux
```

Any agent reading `.clinerules` will branch on the wrong values, causing silent bugs.

---

## Before You Start

1. `git pull origin feat/v0.3.10` in `wilddog64/lib-foundation`
2. Read `.clinerules` — the stale line is in `## Key Contracts`
3. Read `scripts/lib/system.sh` lines 749–776 to confirm current return values
4. Read `scripts/tests/lib/core.bats` lines 46–116 to confirm BATS contract

---

## Change

**Target file:** `.clinerules`

**Old line:**

```
`_detect_platform` returns: `debian | rhel | arch | darwin | unknown`
```

**New line:**

```
`_detect_platform` returns: `mac | wsl | debian | redhat | linux`
```

No other changes.

---

## Rules

- Run `shellcheck scripts/lib/system.sh` — must pass with zero new warnings
- Do NOT modify `scripts/lib/system.sh` or any `.sh` file
- Do NOT commit to `main`

---

## Definition of Done

- [ ] `.clinerules` updated — `_detect_platform` return values corrected
- [ ] No other files modified
- [ ] Committed and pushed to `origin/feat/v0.3.10`
- [ ] Commit message: `docs(.clinerules): fix _detect_platform return values — mac|wsl|debian|redhat|linux`
- [ ] Update `memory-bank/activeContext.md` and `memory-bank/progress.md` with commit SHA and task status
- [ ] Report back: commit SHA + paste the memory-bank lines you updated

## What NOT to Do

- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT modify any `.sh` files
- Do NOT commit to `main`
