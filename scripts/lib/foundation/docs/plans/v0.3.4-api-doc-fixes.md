# Spec: v0.3.4 — API Doc Accuracy Fixes (12 Copilot PR #8 Findings)

**Branch:** `feat/v0.3.4`
**Repo:** `lib-foundation`
**Target files:**
- `docs/api/functions.md`
- `docs/plans/v0.3.3-api-reference.md`

---

## Before You Start

1. Read `memory-bank/activeContext.md` and `memory-bank/progress.md`
2. `git pull origin feat/v0.3.4`
3. Read `docs/api/functions.md` in full
4. Read the relevant function bodies in `scripts/lib/system.sh`, `scripts/lib/core.sh`, `scripts/lib/agent_rigor.sh` to verify each fix before writing it

---

## Context

Copilot PR #8 filed 12 doc accuracy findings against `docs/api/functions.md`. All are
description mismatches — the doc was written before reading the actual function bodies.
No code changes. No BATS changes. Docs only.

---

## Fix 1 — `_detect_platform`: remove `unknown` and `_DETECTED_PLATFORM` caching claim

**File:** `docs/api/functions.md`

The function prints `mac|wsl|debian|redhat|linux` to stdout and calls `_err` on unsupported platforms. It does NOT return `unknown` and does NOT cache anything in `_DETECTED_PLATFORM`.

### Old
```
| `_detect_platform` | Detects the current platform (`mac`, `wsl`, `debian`, `redhat`, `linux`, or `unknown`) and caches it in `_DETECTED_PLATFORM`. |
```

### New
```
| `_detect_platform` | Prints the current platform to stdout: one of `mac`, `wsl`, `debian`, `redhat`, or `linux`. Calls `_err` on unsupported platforms. Does not cache — use `_is_mac` / `_is_linux` etc. for repeated checks. |
```

---

## Fix 2 — `_safe_path`: validates existing PATH, does not construct a new one

**File:** `docs/api/functions.md`

The function iterates over the current `PATH`, collects world-writable or relative entries,
and calls `_err` if any are found. It does NOT construct or return a sanitized PATH.

### Old
```
| `_safe_path` | Construct a sanitized `PATH` that excludes world-writable directories. |
```

### New
```
| `_safe_path` | Validates the current `PATH` for unsafe entries (world-writable directories or relative path components). Calls `_err` with the list of offending entries if any are found. |
```

---

## Fix 3 — `_curl`: no `_safe_path` enforcement; injects `--max-time` default

**File:** `docs/api/functions.md`

The function checks curl exists, injects `--max-time` (default 30 s via `CURL_MAX_TIME`)
if not already specified, and runs via `_run_command --quiet`. No `_safe_path` call.

### Old
```
| `_curl ...` | Wrapper around `curl` that enforces `_safe_path` and tracing rules. |
```

### New
```
| `_curl ...` | Ensures `curl` is installed, injects `--max-time` (default: 30 s via `CURL_MAX_TIME`) when not already specified, and forwards all arguments through `_run_command --quiet`. |
```

---

## Fix 4 — `_ensure_copilot_cli`: mention auth check and error path

**File:** `docs/api/functions.md`

The function installs Copilot CLI (brew → official installer) AND calls `_copilot_auth_check`
after a successful install. Exits via `_err` if installation fails.

### Old
```
| `_ensure_copilot_cli` | Install the GitHub Copilot CLI binary. |
```

### New
```
| `_ensure_copilot_cli` | Ensures the Copilot CLI binary is installed (via `brew install copilot-cli` or the official release installer) and authenticated (`_copilot_auth_check`). Exits via `_err` if installation fails. |
```

---

## Fix 5 — `_cluster_provider`: correct precedence order

**File:** `docs/api/functions.md`

Actual precedence from code: `K3D_MANAGER_PROVIDER` → `K3DMGR_PROVIDER` → `CLUSTER_PROVIDER`
→ auto-detect (k3d binary → k3s binary → k3d default). The current doc has the order wrong.

### Old
```
| `_cluster_provider` | Resolve the active provider from `CLUSTER_PROVIDER` → `K3D_MANAGER_PROVIDER` → `K3DMGR_PROVIDER` → auto-detected default. |
```

### New
```
| `_cluster_provider` | Resolves the active provider in precedence order: `K3D_MANAGER_PROVIDER` → `K3DMGR_PROVIDER` → `CLUSTER_PROVIDER` → auto-detected (`k3d` binary → `k3s` binary → `k3d` default). Normalizes to lowercase; exits via `_err` on unsupported values. |
```

---

## Fix 6 — `_deploy_cluster_resolve_provider`: sets global, does not return

**File:** `docs/api/functions.md`

The function sets `_DCRS_PROVIDER` global. It does not print or return a value.
The current doc says "return a normalized value" which is wrong.

### Old
```
| `_deploy_cluster_resolve_provider <platform> <provider_cli> <force_k3s>` | Determine provider (CLI override, env overrides, mac default) and return a normalized value. |
```

### New
```
| `_deploy_cluster_resolve_provider <platform> <provider_cli> <force_k3s>` | Sets the `_DCRS_PROVIDER` global to the resolved provider: CLI flag → `--force-k3s` → env overrides → mac/interactive/k3d default. Does not print or return a value. |
```

---

## Fix 7 — `create_cluster`: add missing flags and positional ports

**File:** `docs/api/functions.md`

The function accepts `[cluster_name] [http_port=8000] [https_port=8443]` positional args
and `--dry-run/-n` and `--help/-h` flags. The current doc shows only `[cluster_name]`.

### Old
```
| `create_cluster [cluster_name]` | Create infrastructure for the active provider. |
```

### New
```
| `create_cluster [cluster_name] [http_port=8000] [https_port=8443] [--dry-run\|-n] [-h\|--help]` | Create infrastructure for the active provider. `--dry-run` resolves the provider and prints intent without creating. |
```

---

## Fix 8 — `_agent_audit`: remove `[paths...]` from signature

**File:** `docs/api/functions.md`

`_agent_audit` takes no arguments. It operates on staged diffs via git.
The `[paths...]` in the current signature is wrong.

### Old
```
| `_agent_audit [paths...]` | Static audit covering bare `sudo`, if-count thresholds, BATS tests, and credentialed `kubectl exec` usage. |
```

### New
```
| `_agent_audit` | Audits staged diffs for: BATS assertion/test removal, if-count threshold violations (default: 8, configurable via `AGENT_AUDIT_MAX_IF`), bare `sudo` calls, and `kubectl exec` commands with inline credentials. Returns non-zero if any check fails. |
```

---

## Fix 9 — `_agent_lint`: AI-based, not shellcheck; correct gate var

**File:** `docs/api/functions.md`

`_agent_lint` is AI-based — it calls the function named by `AGENT_LINT_AI_FUNC` with
staged `.sh` file names and rules from `scripts/etc/agent/lint-rules.md`.
It does NOT run shellcheck. The `[paths...]` signature is also wrong.

### Old
```
| `_agent_lint [paths...]` | Run `shellcheck` on staged files respecting `AGENT_LINT_GATE_VAR`. |
```

### New
```
| `_agent_lint` | AI-based lint pass on staged `.sh` files. Gated by `AGENT_LINT_GATE_VAR` (default: `ENABLE_AGENT_LINT=1`). Invokes the function named by `AGENT_LINT_AI_FUNC` with staged file names and rules from `scripts/etc/agent/lint-rules.md`. No-op when gate is off or no `.sh` files are staged. |
```

---

## Fix 10 — Global Variables: remove `_DETECTED_PLATFORM` row

**File:** `docs/api/functions.md`

`_DETECTED_PLATFORM` does not exist. `_detect_platform` prints to stdout and does not cache.
Remove the row from the Global Variables table.

### Old
```
| `_DETECTED_PLATFORM` | Cached result of `_detect_platform`; reused by platform helpers. |
```

### New
*(delete the entire row — do not replace with anything)*

---

## Fix 11 — `docs/plans/v0.3.3-api-reference.md`: fix `_cluster_provider` precedence

**File:** `docs/plans/v0.3.3-api-reference.md`

Same wrong precedence order as Fix 5.

### Old
```
| `_cluster_provider` | Resolve active provider from env: `CLUSTER_PROVIDER` → `K3DM_CLUSTER_PROVIDER` → default `k3d` |
```

### New
```
| `_cluster_provider` | Resolves active provider: `K3D_MANAGER_PROVIDER` → `K3DMGR_PROVIDER` → `CLUSTER_PROVIDER` → auto-detected default (`k3d`) |
```

---

## Fix 12 — `docs/plans/v0.3.3-api-reference.md`: fix `_agent_lint` and `_agent_audit` signatures + remove `_DETECTED_PLATFORM` from Global Variables note

**File:** `docs/plans/v0.3.3-api-reference.md`

### Old (agent_rigor table)
```
| `_agent_audit` | `[file...]` | Static audit: bare sudo, if-count, BATS removal, kubectl credential checks |
| `_agent_lint` | `[file...]` | shellcheck on touched files |
```

### New (agent_rigor table)
```
| `_agent_audit` | | Audits staged diffs: bare sudo, if-count, BATS removal, kubectl credential checks |
| `_agent_lint` | | AI-based lint on staged `.sh` files via `AGENT_LINT_AI_FUNC`; gated by `AGENT_LINT_GATE_VAR` |
```

### Old (Global Variables note in Format Rules section)
```
- Add a "Global Variables" section at the end: `_DCRS_PROVIDER`, `_RCRS_RUNNER`, `_DETECTED_PLATFORM` — document what sets them and when they're valid
```

### New (Global Variables note in Format Rules section)
```
- Add a "Global Variables" section at the end: `_DCRS_PROVIDER`, `_RCRS_RUNNER` — document what sets them and when they're valid
```

---

## Rules

- Do NOT modify any file other than `docs/api/functions.md` and `docs/plans/v0.3.3-api-reference.md`
- Do NOT touch any `.sh` files — this is a docs-only fix
- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT commit to `main` — work on `feat/v0.3.4`
- Read the actual function body before applying each fix — verify the description matches reality

---

## Definition of Done

- [ ] All 12 fixes applied to the correct files using the exact old/new strings above
- [ ] `git diff feat/v0.3.4 -- docs/api/functions.md` shows only the 10 targeted lines changed
- [ ] `git diff feat/v0.3.4 -- docs/plans/v0.3.3-api-reference.md` shows only the 4 targeted lines changed
- [ ] No `.sh` files in the diff
- [ ] Committed on `feat/v0.3.4` with exact message:
      `docs(api): fix 12 Copilot PR #8 findings — _detect_platform, _safe_path, _curl, _cluster_provider, _agent_audit, _agent_lint, create_cluster, _DETECTED_PLATFORM`
- [ ] Pushed to `origin/feat/v0.3.4`
- [ ] Update `memory-bank/activeContext.md` and `memory-bank/progress.md`: mark doc fixes as DONE with commit SHA
- [ ] Report back: commit SHA + paste the memory-bank lines you updated

## What NOT to Do

- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT modify `.sh` files or BATS files
- Do NOT commit to `main` — always work on `feat/v0.3.4`
- Do NOT add or remove functions from the doc — only fix the descriptions of existing entries
